# インフラ・デプロイ policy(オンプレ・サブドメイン・環境分離)

> 2026-07-20 更新: アクセス経路が決定。組織で取得済みのドメインがあり、既存Webアプリ群は
> サブドメイン分岐で公開運用されている。本アプリもサブドメイン追加(例: `quiz.○○○.jp`)で公開する。
> これに伴い「ドメイン取得」「Cloudflare Tunnel検討」の節を削除し、既存インフラへの合流手順に書き換え。
>
> **2026-08-24 更新: 基盤が確定。①Cloudflare を使う ②Proxmox に立てた CT(コンテナ)にデプロイする。**
> 7-20時点で「〜らしい」と書いていた推測部分を確定として書き直し、確定したことで新しく発生した
> 検討事項(Docker in LXC / Cloudflare の防御機能 / フロントのビルド時URL)を追記した。
> あわせて `ADMIN_PASSWORD` という古い名前を `ADMIN_TOKEN`・`IMPORT_TOKEN` に修正。

## 前提と方針

- 本番はローカルの**オンプレサーバー**。既存のサブドメイン公開基盤に合流する(ゼロから公開経路を作らない)
- 参加者は会場でQRコード→**自分のモバイル回線**でアクセス(既存アプリが外部公開できている実績が保証)
- 環境は **開発(各自ローカル)/ 本番(オンプレ)** の2つ。ステージングは作らない(本番機で本番前にリハーサルすることで代替)
- **フロントとAPIは1サブドメインに同居させ、パスで分岐する**: `quiz.○○○.jp/` → フロント静的ファイル、`quiz.○○○.jp/api/` → Go。別サブドメインに分けるとCORS設定(ブラウザのセキュリティ機構への対応)が必要になり、初心者チームの定番の詰まりポイントになるため

## 決定: アクセス経路

✅ **既存の組織ドメインのサブドメイン追加方式**(2026-07-20決定)
✅ **公開経路は Cloudflare、実行環境は Proxmox の CT**(2026-08-24確定)

サブドメイン分岐の裏には必ず**リバースプロキシ**(全アクセスの玄関口。サブドメイン名を見て各アプリへ振り分けるサーバー)が存在する。本アプリはその後ろに新しい振り分け先として追加してもらう形になる。

---

## 🔴 最優先: Proxmox の「CT」は VM ではない

**Proxmox の CT は LXC コンテナ**であり、VM とは前提が違う。

| | VM | **CT(LXC)** |
|---|---|---|
| カーネル | 専用のものを持つ | **ホストと共有する** |
| 中で Docker を動かす | 普通に動く | **設定が要る。既定のままでは動かない** |

**CT の中で Docker を動かすには、CT 側の設定に `nesting=1` が必要**(多くの場合 `keyctl=1` も)。無いと `docker compose up` がそもそも起動しない。さらにホストのストレージが **ZFS だと overlayfs が使えず**、`fuse-overlayfs` への切り替えが要る。

これは当日ではなく**デプロイ初日に判明して止まる**種類の問題なので、最初に潰す。

### CT で Docker が動かせない場合の選択肢

| 案 | 内容 | 評価 |
|---|---|---|
| A | CT に Go バイナリと PostgreSQL を直接入れて systemd で動かす | ❌ **非推奨**。開発が compose なので本番だけ構成が変わり、「ローカルでは動くのに」が起きる |
| B | Docker が動く CT(nesting有効)をもらう | ✅ 本命 |
| C | VM をもらう | ✅ 次善。確実だが資源を食う |

**開発と本番で構成を揃えることを優先し、B を第一希望として交渉する。**

---

## インフラ運用者への確認リスト

2026-08-24時点。7-20 の Q1〜Q6 を確定情報に合わせて更新した。

| # | 確認事項 | 状態 / なぜ必要か |
|---|---|---|
| Q1 | **CT で Docker を動かせるか**(`nesting=1` の有無、ホストのストレージが ZFS か) | 🔴 **未確認・最優先**。上記の通り、駄目なら CT/VM の再交渉になる |
| Q2 | Cloudflare の繋ぎ方: 中央の `cloudflared`(Tunnel)に転送先を登録する流儀か、各環境で `cloudflared` を動かす流儀か、Cloudflare がグローバルIPに proxy する流儀か | 🟡 未確認。**Tunnel ならポート開放も証明書も不要**になり、こちらの作業がむしろ減る。後者なら `docker-compose.prod.yml` に `cloudflared` コンテナを1つ足すだけ |
| Q3 | **`quiz.` サブドメインで Bot Fight Mode / Rate Limiting を外せるか** | 🔴 未確認。**当日SSEが全滅する原因になりうる**(下記) |
| Q4 | **`/api/*` をキャッシュしない設定になっているか**(Cache Rule) | 🔴 未確認。SSEやstateがキャッシュされると全端末が古い状態で固まる |
| Q5 | サブドメイン名の希望を伝える(例: `quiz`) | 🟡 **QRコード・ポスターの印刷期限から逆算すること**。締切が一番早い |
| Q6 | CT のスペック(メモリ・コア)、OS、SSHアクセス権を誰がもらえるか | 🟡 200接続を支える前提の確認 |
| Q7 | HTTPS証明書 | ✅ **Cloudflare が終端するので原則不要**。Tunnel なら完全に不要 |

---

## Cloudflare 固有の落とし穴

Cloudflare は「守ってくれる」機能が既定で有効になっていることがあり、**それがそのまま本アプリの障害要因になる**。

### 1. 無通信 100 秒で切断される

Cloudflare は**約100秒データが流れない接続を切る**。プラン設定で変更できない。

→ **Go 側で 15〜30 秒ごとにハートビート(コメント行)を流すことが必須。**プロキシ設定に依存しない唯一の安全策なので、これは実装で担保する(API・データ設計policy の SSE 仕様に組み込み済み)。

### 2. Bot Fight Mode / WAF / Rate Limiting

**会場で約200台がほぼ同時に同じURLへ接続する**ので、Cloudflare 側から見ると攻撃と区別がつかない。

- チャレンジ(JS判定)が出ると、**`EventSource` はそれを通過できない**ため SSE が全滅する
- 組織の共有ドメインの場合、**既に Rate Limiting ルールが掛かっている可能性がある**
- **「I'm Under Attack」モードは絶対に有効にしない**

→ Q3 で `quiz.` サブドメインだけ除外できるか確認する。

### 3. キャッシュ

`/api/*` が Cloudflare にキャッシュされると、全端末が同じ古いJSONを見続ける。SSE 応答には次を付ける。

```
Cache-Control: no-cache
X-Accel-Buffering: no
```

加えて **Cloudflare 側の Cache Rule で `/api/*` を Bypass に**する(Q4)。

---

## SSE特有の落とし穴(本番だけ動かない事故の防止)

SSEは「接続を開きっぱなしで流し続ける」通信のため、普通のWeb向けデフォルト設定のリバースプロキシと相性問題が2つある:

1. **バッファリング**: プロキシが応答を溜めてから送る設定だと、イベントが即座に届かない(=全端末の画面が切り替わらない)。該当サブドメインの設定で無効化が必要(nginxなら `proxy_buffering off`)
2. **無通信タイムアウト**: 「60秒データが流れない接続は切断」が一般的なデフォルト。問題と問題の間で全員の接続が切れる。**対策はGo側のハートビート**(上記)

症状が「ローカルでは動くのに本番だけ画面が切り替わらない」になるため、知らないと当日デバッグ不能になる。**本番サブドメインでのSSE疎通確認を、リハーサルより前の早い段階で1回やる**こと。

> **これは #77(SSE配信)の完成を待たなくてよい。**中身が空でも、`text/event-stream` が Cloudflare を越えて流れ続けることさえ確認できれば、上記のリスクはほぼ消える。**最優先で通す。**

---

## 本番のコンテナ構成

```
Cloudflare(既存・運用者管理)
   │ quiz.○○○.jp へのアクセスを振り分け
   ▼
Proxmox の CT
   │
   ├─ nginx ── /     → フロントのビルド済み静的ファイルを配信
   │           /api/ → backendへ中継(SSE用の buffering off 設定込み)
   ├─ backend(Go)
   └─ db(PostgreSQL)
```

開発時の3コンテナ(frontend/backend/db)と違い、本番はフロントが「Viteでビルドした静的ファイル+nginx配信」になる(`docker-compose.prod.yml` で定義)。

> ⚠️ **いまの `frontend/Dockerfile` は開発専用**(`CMD ["pnpm", "dev", "--host"]`)。本番用に「ビルドして nginx に載せる」multi-stage の Dockerfile が**別途必要**。

---

## 🔴 フロントに `localhost:3000` が焼き込まれる事故

**一番やらかしやすい。**`frontend/src/lib/config.ts`:

```ts
export const BASE: string = import.meta.env.VITE_API_URL
if (!BASE) { throw new Error('VITE_API_URL が設定されていません。...') }
```

**Vite の環境変数は「ビルドした瞬間の値」がJSに文字列として埋め込まれる。**実行時に読み直さない。本番ビルド時に `VITE_API_URL=http://localhost:3000` のままだと、**参加者のスマホが自分自身の3000番を叩きに行って全滅する。**

しかも `if (!BASE) throw` があるため、**空文字にして相対パス(`/api/...`)にする逃げ道が塞がれている。**

| 案 | 内容 | 評価 |
|---|---|---|
| **A-1** | `config.ts` を「未設定なら相対パス(`''`)」に直す | ✅ **推奨**。同一サブドメイン構成なので `/api/state` で正しく届き、**ビルド時に本番URLを知る必要がなくなる** |
| A-2 | ビルド時に `VITE_API_URL=https://quiz.○○○.jp` を必ず渡す | 手順を毎回守る必要があり、間違えても**ビルドは成功してしまう** |

**A-1 を採る。**「手順を守る」ではなく「間違えられない形にする」ほうが当日の安全性が高い。

---

## 開発環境と本番環境の分け方

やることは3つ:

1. **設定値を全部 環境変数にする**(コードに書き込まない)
   - `.env.example` に一覧、開発は `docker-compose.yml` に直書き、本番はサーバー上の `.env.prod`
   - 例: `DATABASE_URL`, `ADMIN_TOKEN`, `IMPORT_TOKEN`, `SPREADSHEET_ID`, `GOOGLE_CREDENTIALS_JSON`
   - **`.env.prod` は git に入れない。**パーミッションは `600`
2. **composeファイルを分ける**
   - 開発: `docker-compose.yml`(bind mount + ホットリロード)
   - 本番: `docker-compose.prod.yml`(ビルド済みイメージ・mount無し・`restart: always`・上記のnginx構成)
   - 起動: `docker compose -f docker-compose.prod.yml up -d`
3. **本番だけの手順を `docs/ガイドライン/デプロイ手順.md` に書く**(デプロイ=作業手順書があること)

### トークンの扱い

- **本番の `ADMIN_TOKEN` / `IMPORT_TOKEN` はイベント直前に発行し、終了後に無効化する**(使い捨て)
- 開発用と本番用は**必ず違う値**にする
- SSE は仕様上トークンがURLに載るため、**ログ・履歴・スクショからの漏洩を前提**に設計する
- **管理者画面のパス `/backstage-0248` は秘密ではない**。トークンだけが秘密

---

## デプロイ方式

v1は**手動デプロイで十分**(CDは作らない):

```
CTにSSH → git pull → docker compose -f docker-compose.prod.yml up -d --build → migrate実行
```

これを `mise run deploy`(本番CT上で実行するタスク)にまとめる。自動化(GitHub Actionsからのデプロイ)は動いてから考えるv2。

**マイグレーションはアプリ起動前に流す**こと(順序を手順書に明記)。

---

## Proxmox だからこそ使える保険: スナップショット

**CT のスナップショットをイベント前に取っておくこと。**Proxmox を使う最大の利点。

- **リハーサル完了時点**で1つ
- **当日の朝、問題データ投入後**に1つ

「設定をいじって壊した」時に、**数十秒で丸ごと戻せる**。`pg_dump` と違い DB だけでなく設定ごと戻るため、当日の復旧手段としては最も確実。**`pg_dump` と併用する**(スナップショットは戻しすぎるので、データだけ戻したい時に `pg_dump` が要る)。

---

## 本番運用で最低限決めておくこと

| # | 項目 | 状態・推奨 |
|---|---|---|
| I1 | アクセス経路 | ✅ サブドメイン追加 + Cloudflare + Proxmox CT(2026-08-24確定)。残タスクは確認リスト Q1〜Q6 |
| I2 | DBバックアップ | イベント前に `pg_dump` を1回 + **CTスナップショット**。クイズデータはスプシから再同期できるので喪失リスクは低い |
| I3 | 当日の障害対応 | 「CT再起動手順」「進行をリセットして途中の問題から再開する手順」を手順書に書く。**紙に印刷して当日持つ**。`EventState` がDBにあるので再起動後も復帰できる設計 |
| I4 | 管理者画面の保護 | トークン認証(#10で実装済み)+ 推測されにくいパス(`/backstage-0248`)。可能ならアクセス元制限 |
| I5 | 負荷 | **SSE同時接続 = 参加人数分。想定 約200人**(R5確定)。**本番経路で**200接続テストを1回やる。Go側は余裕だが、詰まるとすれば Cloudflare(上記)と **CTのファイルディスクリプタ上限(`ulimit -n`)**。LXCはホストの制限を受ける |
| I6 | **時刻同期(NTP)** | 🆕 `serverTime` を全端末のカウントダウンに使うため、CTの時計がずれると残り時間が全員ずれる。NTPが効いているか確認する |
| I7 | **タイムゾーン** | 🆕 コンテナ既定は UTC。動作に影響はないが、**当日ログを読むとき JST でないと辛い**。`TZ=Asia/Tokyo` を入れるか判断する |
| I8 | ログ | 🆕 当日その場でログを見る手段を決めておく(`docker compose logs -f`)。**トークンを絶対にログに出さない**(必要なら長さだけ) |
| I9 | **ネット断のフォールバック** | 🆕 会場回線か Cloudflare が落ちた時どうするか。**「紙で進行する」も立派な回答**。決めて周知しておくこと |

---

## 段取り(いつやるか)

順番が重要。**アプリの完成を待つ項目を後ろに置かない。**

| 順 | やること | 待つ必要のあるもの |
|---|---|---|
| ① | **CT で Docker が動くことの確認** | なし → **今すぐ** |
| ② | **本番サブドメインに空のSSEを1本通す** | なし(中身は空でよい) → **早期に** |
| ③ | `docker-compose.prod.yml` と本番用 `frontend/Dockerfile` を書く | ①の結果 |
| ④ | 200接続の負荷テスト(本番経路で) | ③ |
| ⑤ | 通しリハーサル | Phase 2完了 |
| ⑥ | 本番トークン発行・問題データ投入・スナップショット | 当日 |

**②を「アプリが完成してから」にすると、当日デバッグ不能になる。**

---

## TODO(開発と並行して進める)

- [ ] 🔴 【最優先】インフラ運用者に **Q1(CTでDockerが動くか)** を確認する
- [ ] 🔴 Q3・Q4(Bot Fight Mode / Rate Limiting / `/api/*` のキャッシュ)を確認する
- [ ] 🟡 Q2(Cloudflareの繋ぎ方)、Q5(サブドメイン名)、Q6(スペック・SSH)を確認する
- [ ] 🟡 **Q5 はポスター・QR印刷の締切から逆算**して、他より先に確定させる
- [ ] `config.ts` を「未設定なら相対パス」に直す(§フロントに localhost が焼き込まれる事故)
- [ ] 空のSSEを本番サブドメインに通し、**Cloudflare越しの疎通を確認**する(早期に)
- [ ] `docker-compose.prod.yml` / 本番用 `frontend/Dockerfile` / `docs/ガイドライン/デプロイ手順.md` の雛形を作る
- [ ] 本番1ヶ月前リハーサル(Phase 2完了時)を予定に入れる
